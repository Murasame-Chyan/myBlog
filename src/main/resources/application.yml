server:
  port: 8080

spring:
  application:
   name: myBlog

  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/myblogdb
    username: root
    password: CHANGED_DB_PASSWORD
    driver-class-name: com.mysql.cj.jdbc.Driver

