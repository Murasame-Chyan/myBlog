# dev profile — 本地开发时会激活此配置
# 敏感值全部来自外部配置或环境变量，此文件只放 dev 特有的调试开关

spring:
  devtools:
    restart:
      enabled: true

logging:
  level:
    com.murasame: DEBUG

security:
  cookie:
    secure: false
