# dev profile — 本地开发时激活
# jwt 的 dev 覆盖已移入 application.yml 多文档（spring.config.activate.on-profile: dev）
# 此文件仅保留 devtools 和日志等配置

spring:
  devtools:
    restart:
      enabled: true

logging:
  level:
    com.murasame: DEBUG

security:
  cookie:
    secure: false
